# 对话回顾 · 2026-08-01 15:30

> **Source**: [conversation-review-skills](https://github.com/wangguoxi0814/conversation-review-skills)
> **Generated by**: conversation-review skill

---

## 问题列表

1. **SQLAlchemy 中 flush 和 commit 的区别，以及事务何时提交**
   > 原问题：「这里为什么要flush」「那这里的插入什么时候执行commit」

   > 原问题：Response 对象的 json() 方法是干嘛的？

   关键点：
   - `json()` 是 HTTP 响应对象的便捷方法
   - 自动解析响应体中的 JSON 字符串为 Python 字典
   - 内部调用 `json.loads(self.text)`
   - 如果响应不是有效 JSON 会抛出异常

2. **使用 `await response.json()` 解析响应数据**

   > 原问题：怎么解析返回的数据？
   > 原问题：这个 json() 怎么用？
   > 原问题：`app/dependencies.py:15-28` 这个 json() 怎么用？

   关键点：
   - `response.json()` 自动解析 JSON 响应为 Python 对象
   - 需要 `await` 因为是异步操作
   - 解析失败会抛出 `JSONDecodeError`

3. **JSON 是 API 数据交换的通用格式**

   > 原问题：返回的 JSON 是什么？

   关键点：
   - JSON (JavaScript Object Notation) 是轻量级数据格式
   - 是 Web API 最常用的数据交换格式
   - 支持字符串、数字、数组、对象等数据类型

4. **API 和 HTTP 的关系**

   > 原问题：API 和 HTTP 有什么关系？

   关键点：
   - HTTP 是传输协议，API 是接口规范
   - RESTful API 通常基于 HTTP 方法（GET/POST/PUT/DELETE）
   - API 定义端点（URL），HTTP 负责数据传输

5. **async/await 的含义和用法**

   > 原问题：async/await 是什么意思？
   > 原问题：为什么有些函数定义是 async def，有些不是？
   > 原问题：await 是必须的吗？

   关键点：
   - `async def` 定义协程函数，调用时不会立即执行
   - `await` 暂停当前任务，等待异步操作完成
   - 只能在 `async` 函数内使用 `await`
   - 适合 I/O 密集型任务（网络请求、文件操作）

6. **`httpx.AsyncClient` 的用途和配置**

   > 原问题：这个 client 是干嘛的？
   > 原问题：为什么要用 AsyncClient 而不是直接 requests？

   关键点：
   - `httpx.AsyncClient` 是异步 HTTP 客户端
   - 支持 HTTP/2、连接池、超时配置
   - 比 `requests` 更现代，支持异步操作
   - 使用 `async with` 确保连接正确关闭

7. **FastAPI 的依赖注入系统**

   > 原问题：Depends 是什么？
   > 原问题：这个函数参数怎么自动就有了？

   关键点：
   - `Depends` 是 FastAPI 的依赖注入机制
   - 自动解析并注入函数声明的依赖
   - 支持嵌套依赖和自动参数解析
   - 提高代码复用性和可测试性
